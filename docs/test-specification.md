# テスト仕様書 — Summary Slackbot

## テスト概要

| カテゴリ | テスト数 | 正常 | 異常 | 境界 |
|---------|---------|------|------|------|
| 認証（AUTH） | 15 | 6 | 6 | 3 |
| 要約（SUM） | 16 | 5 | 8 | 3 |
| 履歴（HIST） | 10 | 5 | 3 | 2 |
| Slack投稿（SLK） | 6 | 2 | 3 | 1 |
| API共通（API） | 8 | 2 | 4 | 2 |
| UI/レスポンシブ（UI） | 5 | 4 | 0 | 1 |
| ヘルスチェック（HC） | 1 | 1 | 0 | 0 |
| **合計** | **61** | **25** | **24** | **12** |

---

## 1. 認証（AUTH）

| ID | 種別 | テスト内容 | 期待結果 |
|----|------|----------|---------|
| AUTH-01 | 正常 | 有効なメール+パスワード(8文字以上)で新規登録 | 登録成功、確認メールが届く |
| AUTH-02 | 正常 | 確認メールのリンクをクリック | メール認証完了、ログイン可能になる |
| AUTH-03 | 正常 | 登録済みメール+正しいパスワードでログイン | ログイン成功、トップ画面に遷移 |
| AUTH-04 | 正常 | ログアウトボタンをクリック | ログアウト、ログイン画面に遷移 |
| AUTH-05 | 正常 | パスワードリセットで有効なメールを入力 | リセットメールが届く |
| AUTH-06 | 正常 | リセットメールから新しいパスワードを設定 | パスワード変更完了、新パスワードでログイン可能 |
| AUTH-07 | 異常 | メール欄を空でログインボタンを押す | 「メールアドレスを入力してください」表示 |
| AUTH-08 | 異常 | パスワード欄を空でログインボタンを押す | 「パスワードを入力してください」表示 |
| AUTH-09 | 異常 | 存在しないメールでログイン | 「メールアドレスまたはパスワードが正しくありません」表示 |
| AUTH-10 | 異常 | 正しいメール+間違ったパスワードでログイン | 「メールアドレスまたはパスワードが正しくありません」表示 |
| AUTH-11 | 異常 | 登録済みメールで再度新規登録 | 「このメールアドレスは既に登録されています」表示 |
| AUTH-12 | 異常 | メール未認証の状態でログイン | 「メールアドレスの認証が完了していません」表示 |
| AUTH-13 | 境界 | パスワード7文字で新規登録 | 「パスワードは8文字以上で入力してください」表示 |
| AUTH-14 | 境界 | パスワード8文字ちょうどで新規登録 | 登録成功 |
| AUTH-15 | 境界 | 不正な形式のメール(例: "abc")で新規登録 | 「正しいメールアドレスを入力してください」表示 |

---

## 2. 要約（SUM）

| ID | 種別 | テスト内容 | 期待結果 |
|----|------|----------|---------|
| SUM-01 | 正常 | 有効なURLを入力してレベル「普通」で要約 | 5〜8行の要約が画面に表示される |
| SUM-02 | 正常 | 有効なURLを入力してレベル「簡単」で要約 | 3行程度の要約が画面に表示される |
| SUM-03 | 正常 | 有効なURLを入力してレベル「詳しく」で要約 | 15行程度の要約が画面に表示される |
| SUM-04 | 正常 | レベル未選択（デフォルト）で要約 | デフォルト「普通」で要約される |
| SUM-05 | 正常 | 要約生成ボタンをクリック | ローディング表示「記事の要約を生成しています...」が出る |
| SUM-06 | 異常 | URL欄を空で要約ボタンを押す | 「URLを入力してください」表示 |
| SUM-07 | 異常 | 無効なURL形式(例: "abcdef")を入力 | 「正しいURLを入力してください」表示 |
| SUM-08 | 異常 | 存在しないURL(404)を入力 | 「このページは存在しません」表示 |
| SUM-09 | 異常 | スクレイピング不可のURL(例: ログイン必須ページ) | 「この記事の内容を取得できませんでした」表示 |
| SUM-10 | 異常 | LLM APIキーが無効な状態で要約 | 「要約の生成に失敗しました。もう一度お試しください」表示 |
| SUM-11 | 異常 | タイムアウト(10秒超過)する長い記事 | 「要約の生成がタイムアウトしました」表示 |
| SUM-12 | 異常 | LLM_PROVIDER=anthropicでClaude APIエラー | エラーメッセージ表示、画面が真っ白にならない |
| SUM-13 | 異常 | LLM_PROVIDER=openaiでOpenAI APIエラー | エラーメッセージ表示、画面が真っ白にならない |
| SUM-14 | 境界 | URLのみ（テキストなし）のページを要約 | 「この記事の内容を取得できませんでした」表示 |
| SUM-15 | 境界 | 非常に長い記事(1万文字以上)を要約 | タイムアウトせず正常に要約される |
| SUM-16 | 境界 | LLM_PROVIDER=anthropicとopenaiを切り替えて要約 | 両方で正常に要約が生成される |

---

## 3. 履歴（HIST）

| ID | 種別 | テスト内容 | 期待結果 |
|----|------|----------|---------|
| HIST-01 | 正常 | 要約後に履歴一覧画面を開く | 直前の要約が一覧の先頭に表示される |
| HIST-02 | 正常 | 複数回要約後に履歴一覧を開く | 新しい順にソートされて一覧表示 |
| HIST-03 | 正常 | 履歴一覧から1件をクリック | 詳細画面に遷移し、タイトル・要約・URL・日時が表示 |
| HIST-04 | 正常 | 履歴詳細画面の元URLリンクをクリック | 元記事が新しいタブで開く |
| HIST-05 | 正常 | 過去に要約済みのURLを再度入力 | 「この記事は以前に要約しています。再要約しますか？」表示 |
| HIST-06 | 異常 | 重複URL確認で「過去の要約を見る」を選択 | 該当の履歴詳細画面に遷移 |
| HIST-07 | 異常 | 重複URL確認で「キャンセル」を選択 | トップ画面に戻る |
| HIST-08 | 異常 | 存在しない履歴ID(/history/invalid-id)にアクセス | 「要約が見つかりません」表示 |
| HIST-09 | 境界 | 履歴0件の状態で履歴一覧を開く | 「まだ要約履歴がありません」表示 |
| HIST-10 | 境界 | 重複URL確認で「再要約する」を選択 | 新しい要約が生成され、履歴に追加される |

---

## 4. Slack投稿（SLK）

| ID | 種別 | テスト内容 | 期待結果 |
|----|------|----------|---------|
| SLK-01 | 正常 | 要約完了後にSlackチャンネルを確認 | 要約がSlackに投稿されている |
| SLK-02 | 正常 | Slack投稿のフォーマットを確認 | タイトル、要約、URL、レベル、要約者が含まれている |
| SLK-03 | 異常 | SLACK_WEBHOOK_URLが無効な状態で要約 | 「Slackへの投稿に失敗しました」表示、要約自体は保存される |
| SLK-04 | 異常 | SLACK_WEBHOOK_URLが未設定の状態で要約 | 「Slackへの投稿に失敗しました」表示、要約自体は保存される |
| SLK-05 | 異常 | Slackのrate limit(レート制限)に引っかかった場合 | エラーメッセージ表示、要約は保存される |
| SLK-06 | 境界 | 非常に長い要約（詳しくレベル）のSlack投稿 | 投稿が途切れず全文表示される |

---

## 5. API共通（API）

| ID | 種別 | テスト内容 | 期待結果 |
|----|------|----------|---------|
| API-01 | 正常 | 有効なトークンでAPIリクエスト | 正常にレスポンスが返る |
| API-02 | 正常 | 他ユーザーの履歴IDで詳細取得 | 404 Not Found（他人のデータは見えない） |
| API-03 | 異常 | トークンなしでAPIリクエスト | 401 Unauthorized |
| API-04 | 異常 | 無効なトークンでAPIリクエスト | 401 Unauthorized |
| API-05 | 異常 | 期限切れトークンでAPIリクエスト | 401 Unauthorized |
| API-06 | 異常 | 不正なJSONボディでPOSTリクエスト | 400 Bad Request |
| API-07 | 境界 | POST /api/summarize に GET でリクエスト | 405 Method Not Allowed |
| API-08 | 境界 | 存在しないAPIパスにリクエスト | 404 Not Found |

---

## 6. UI / レスポンシブ（UI）

| ID | 種別 | テスト内容 | 期待結果 |
|----|------|----------|---------|
| UI-01 | 正常 | PC画面(1280px以上)で各画面を確認 | 全画面が正しく表示される |
| UI-02 | 正常 | タブレット画面(768px)で各画面を確認 | レイアウトが崩れない |
| UI-03 | 正常 | スマホ画面(375px)で各画面を確認 | レイアウトが崩れない、ナビがハンバーガーメニューに変わる |
| UI-04 | 正常 | ヘッダーのナビリンク（トップ/履歴/設定/ログアウト）をクリック | 各画面に正しく遷移する |
| UI-05 | 境界 | ブラウザの戻るボタンで前画面に戻る | 画面が壊れずに正しく表示される |

---

## 7. ヘルスチェック（HC）

| ID | 種別 | テスト内容 | 期待結果 |
|----|------|----------|---------|
| HC-01 | 正常 | GET /api/health にリクエスト | `{ "status": "ok" }` が返る |

---

## 8. アカウント削除（DEL）

※ 認証カテゴリとは別に、削除フロー専用のテストを記載

| ID | 種別 | テスト内容 | 期待結果 |
|----|------|----------|---------|
| DEL-01 | 正常 | 設定画面で「アカウント削除」→「削除する」 | アカウント削除、ログイン画面に遷移 |
| DEL-02 | 正常 | 削除後、同じメールでログイン試行 | 「メールアドレスまたはパスワードが正しくありません」表示 |
| DEL-03 | 異常 | 「アカウント削除」→「キャンセル」 | 何も起きず設定画面のまま |
| DEL-04 | 正常 | アカウント削除後、要約履歴がDBから消えていることを確認 | summariesテーブルに該当ユーザーのデータがない |
